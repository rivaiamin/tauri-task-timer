{
  "name": "task-timer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build": "node build.js",
    "predeploy": "npm run build",
    "preview": "npm run build && npx http-server dist"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.4"
  }
}
